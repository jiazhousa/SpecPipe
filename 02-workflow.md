# 02-workflow —— 流程总览与公共环节

本卷定义三条路径的总览、S0~S2 公共环节与用户放行机制。适用阶段：任何需求进入工作流时首先加载；S2 分级判定的权威卷。

调度者驱动的编码流程：用户只需在**访谈澄清、分级确认、审查不通过**时参与，其余全自动推进；工作流根据需求规模自动分流为 Epic / Story / Issue 三条路径。

## 三路径总览

```
S0 调研
 → S1 前置访谈（用户参与，定设想+范围+改动点）
 → S2 分级判定（统一调度点，允许等级重调整，用户确认）
  ──┬─ Epic  ─→ E-S3 → E-S4 → E-S5 → 用户放行 → 按路线图逐个 Story 从 S0 开始
    ├─ Story ─→ S-S3 → S-S4 → S-S5 → 用户放行 → S-S6 → S-S7 → S-S8 → S-S9 编码 → S-S10 质量门
    └─ Issue ─→ I-S3 Impl → I-S4 澄清 → I-S5 审查 → I-S6 编码 → I-S7 质量门
```

> **S2 是统一调度点** — 调研 + 访谈结束后，调度者做等级判定。若发现实际需求与初步判定不符，可在此时**提议升级或降级**，经用户确认后调整路径。详见下文「S2 分级判定」。

> **Issue 不走 spec 阶段** — Issue 级别的需求在 S1 前置访谈中就把改动点聊清楚，S2 确认后直接产出 Impl 文档 → 审查 → 编码 → 质量门。无 Spec 文档、无 Spec 审查。

> **质量门（S-S10 / I-S7）** 是所有编码路径的**终检环节**，在提交到远端前由审查者执行全面审查（7 项），通过后才推送分支。详见 09-check-split.md。

三条路径详情：Story 见 03-story-path.md，Epic 见 04-epic-path.md，Issue 见 05-issue-path.md。

## 用户参与点

用户只在以下场景参与，其余环节全自动推进：

- 前置访谈（S1，定设想 + 范围 + 改动点）
- 分级确认与等级重调整（S2）
- Epic Spec 澄清与放行（E-S4 / E-S5 后）
- Story Spec 澄清与放行（S-S4 / S-S5 后）
- Story Impl 澄清（S-S7）
- Issue Impl 澄清（I-S4）
- 审查推翻 spec 决策（SPEC_OVERTURN）

## 公共环节

### S0 调研

调度者调用**调研者**（下级代理）执行：

- 任务 A：内部代码库调研（grep 搜索相关代码，理解现有实现）
- 任务 B：外部技术调研（主搜索 / 备选搜索 / 文档查询三工具，职责见 08-roles.md「检索工具」）

调度者可在同一调研阶段**发起多个调研者任务**（下级代理调用机制支持并行）。产出保留在对话上下文，不写文件。调用机制与任务书规范见 08-roles.md。

### S1 前置访谈（用户参与）

调度者基于调研产出，识别需求中的模糊点，**分批向用户提问**（每次 1-3 个问题），逐轮澄清。

**前置访谈的目的**是确定**用户的设想和改动范围**，为分级判定收集足够信息。对于 Issue 级别的需求（Bug fix、小调整），前置访谈需**聊清所有涉及的具体改动点**（哪些文件、什么改法、边界条件），因为 Issue 路径不设 Spec 阶段，访谈是确认需求的唯一环节。对于 Story/Epic 级别的需求，业务细节在 S-S4/E-S4 环节完成，前置访谈只需确定设想和范围。

访谈完成后进入 **S2 分级判定**。

### S2 分级判定（统一调度点，用户确认）

S2 是**统一调度点**——调研 + 访谈结束后，调度者做等级判定。此处**允许等级重调整**：

调度者基于 S0 调研 + S1 前置访谈结果，按 01-general.md「需求分级」表判定级别，向用户输出：

> **需求级别判定：[Epic / Story / Issue]**
> 判定理由：...
> 建议路径：[Epic 路径 / Story 路径 / Issue 路径]

**等级重调整场景**：

- **升级** — 初步判定为 Issue，但访谈中发现涉及 >3 文件或引入新业务规则 → 调度者主动提议升级为 Story
- **升级** — 初步判定为 Story，但访谈中发现跨多模块、含多个可独立交付子需求 → 提议升级为 Epic
- **降级** — 初步判定为 Story/Epic，但访谈中发现实际改动范围很小 → 提议降级为 Issue
- **用户主动调整** — 用户对分级有异议，按用户意见调整

重调整时调度者需说明**调整理由**（哪些事实与原级别判定标准不符），经用户确认后进入调整后的路径。

**此环节阻塞，调度者不自动推进。**

### 用户放行机制（适用于所有阻塞点）

工作流中有两处阻塞点需要**用户放行**：E-S5 后的 Epic Spec 放行、S-S5 后的 Story Spec 放行。调度者在阻塞状态下向用户输出审查通过的 spec 终稿摘要，并明确询问"是否放行进入下一阶段？"。

**放行判定规则**：

- 用户回复"放行"/"通过"/"同意"/"继续"/"OK"/"可以" 等肯定语义 → 调度者判定为放行，推进流程
- 用户回复"修改"/"不改"/"有意见"/"等等" 等否定或犹豫语义 → 调度者继续听取用户意见，根据反馈修改 spec 后重新提交审查（回到对应审查环节）
- 用户提出新的修改要求 → 调度者修改 spec 后重新提交审查
- **不自动推进**：调度者不得在用户未明确回复时自行判定放行
