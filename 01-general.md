# 01-general —— 总则与配置

本卷定义工作流的总则硬条文、触发条件、需求分级标准与配置项清单。适用阶段：全流程常读；S2 分级判定前必读。

## 总则硬条文

1. **调度者驱动全流程** — 无需用户手动调用命令，调度者按阶段自动推进；编码执行派发给执行者（下级代理）。
2. **Plan 阶段不编码** — spec 与 impl 均审查通过前不修改业务代码（Issue 路径无 spec，impl 审查通过前不编码）。
3. **所有文档和记忆使用中文**。
4. **放行不自动推进** — 阻塞点处调度者不得在用户未明确回复时自行判定放行。
5. **impl.md 是唯一事实源** — 派发编码的任务书不内嵌实现细节，只指定章节 + 验证命令 + 交付物。impl 写不清楚导致执行者做错，责任在调度者（文档质量）——这倒逼 impl 阶段做实。
6. **异家族互查** — 审查者与执行者由不同家族的模型担任，交叉验证，避免单模型自审盲区。
7. **最小验证原则** — 单个编码任务块只执行任务书指定的最小验证命令（如仅编译受影响模块、仅跑指定新增测试类、前端只查语法）；全量测试 / E2E / fence（测试围栏）由调度者在 Story 收尾统一执行，不在任务块中重复。

## 工作流触发

当调度者收到用户的开发需求时，**默认进入工作流**（分级判定会自动区分 Issue / Story / Epic，无需以"功能级开发"为触发门槛）。

- 用户也可直接说"走编码工作流"显式触发
- 极简任务（单行改动、纯文档）调度者可自行判断不走工作流，但需知会用户

## 需求分级

调度者在 **S0 调研 + S1 前置访谈**完成后，基于调研产出和访谈结果判定需求规格级别。分级标准：

| 级别 | 判定特征 | 走哪条路径 |
|------|---------|-----------|
| **Epic** | 跨多模块、多团队协作；含 ≥3 个可独立交付的子需求；周期 >2 周；涉及架构演进或业务流程重构 | Epic 路径（环节 E- 前缀，见 04-epic-path.md） |
| **Story** | 单模块内完整功能；有明确业务规则和验收标准；周期 3 天-2 周；需 spec + impl 两阶段设计 | Story 路径（环节 S- 前缀，见 03-story-path.md） |
| **Issue** | Bug fix、小功能性调整、单点优化；改动范围 ≤3 文件；无新业务规则引入；周期 ≤2 天 | Issue 路径（环节 I- 前缀，见 05-issue-path.md） |

**判定由调度者自行完成**，不调用额外角色。判定后向用户说明级别和理由，并进入对应路径。

## 配置项清单

工作流的可配置项分五组。本卷只定义配置项名与联动规则；具体值（模型名、目录名、分支名等）属个人配置域，不在规章中约定，配置机制归实现仓。

| 组 | 配置项 | 联动规则 |
|----|--------|---------|
| 工作流根目录 | 根目录名（`{wf}` 占位符的解析目标） | 修改后，审查者的报告与状态写入路径白名单需同步更新 |
| 各角色模型 | 调度者 / 调研者 / 审查者 / 执行者 / 视觉解析者（可选）各自的模型与提供方 | 模型可在代理定义与运行配置两处设置，冲突时以运行配置为准；视觉解析者模型须支持图片输入 |
| 检索工具 | 主搜索 / 备选搜索 / 文档查询 | 主搜索额度耗尽时降级备选搜索；三者均为只读查询 |
| Git 三分支 | 开发主分支 / 发布测试分支 / 生产分支 | 编码分支一律从开发主分支拉出；发布流程见 10-composition.md |
| 临时目录 | 后台会话日志目录 | 可持久化后台会话的日志落盘位置 |

## 路径约定 `{wf}`

本规章所有工作流产出路径均以 `{wf}/` 作为工作流根目录占位符，默认映射为 `.specpipe/`（项目根目录下），可通过配置修改。例如默认配置下：

- `{wf}/plans/{topic}/spec.md` = `.specpipe/plans/{topic}/spec.md`

`{topic}` 为当前需求的工作目录名，取值规则见 06-artifacts.md。

## 分卷加载

本规章按卷组织，**卷粒度 = 加载单位**：调度者与各下级代理在进入对应工作阶段时按需加载相关卷，不要求一次载入全仓。典型加载场景：

- 分级判定：01 + 02
- Story 路径：02 + 03（审查环节加 09，编码环节加 10）
- Epic 路径：02 + 04（进入 Story 推进后加 03）
- Issue 路径：02 + 05
- 产出工件：06 + `templates/`
- 派发下级代理：08
