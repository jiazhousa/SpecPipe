# 08-roles —— 角色与调度协议

> 本卷职责：五角色职责矩阵、角色定位、任务书标准、调度协议与检索工具职责。适用阶段：全流程各派发环节（调研 / 审查 / 编码）。

## 五角色职责矩阵

| 角色 | 状态 | 流水线位置（上下游） | 职责 | 输入 → 输出 |
|---|---|---|---|---|
| **调度者** | **有状态**（会话 + `.stage`） | 顶点：用户 ↔ 调度者 ↔ 各下级代理 | 工作流状态机掌控、需求访谈与拆解、spec/impl 文档产出、任务切分与派发、冲突调节与进度把控、向用户汇报；**不直接编码**（串行小任务可酌情例外） | 用户需求、下级代理报告、`.stage` 状态 → 任务书、spec/impl 文档、派发决策、用户汇报 |
| **调研者** | **无状态**（每次新建） | 上游调度者 → 下游调度者 | 内部代码库调研 + 外部技术调研（事实清单，区分事实/推断） | 调研任务书（需求 + 范围 + 代码库路径）→ 结构化调研报告（带 文件:行号），不写文件 |
| **审查者** | **无状态**（每次新建） | 上游调度者（间接：执行者产出被审）→ 下游调度者 | 质量卡点：完成状态检查 + 交付质量检查 + 质量门全面审查（含编译/测试执行）+ 审查结果落定 | 审查任务书（类型 + 对象路径 + 基准材料）→ 审查报告（`reviews/` 落盘）+ `.stage` 更新 |
| **执行者** | **无状态**（每次新建，可并行） | 上游调度者 → 下游调度者（产出终由审查者审） | 按 impl.md 任务书编码执行：读文档 → 写码 → 最小自验 → 报告；不做需求分析/方案设计/质量审查 | 编码任务书（impl.md 章节 + worktree + 验证命令）→ 代码变更（worktree 内）+ 标准执行报告，不写文件 |
| **视觉解析者**（可选） | **无状态**（每次新建） | 上游调度者 → 下游调度者 | 图片解析：截图/设计稿/架构图/报错照片 → 结构化文字描述（含"无法确认项"，禁止编造）；仅当调度者模型不支持图片输入时部署 | 解析任务书（图片绝对路径 + 解析目标）→ 结构化图片描述，不写文件 |

> 各角色的模型选择属个人组合配置，本卷不规定（组合与覆盖规则见 10-composition.md）。

## 角色定位

1. **调度者是有状态的调度中枢** — 主会话长期存在，承载 `.stage` 状态机、用户访谈、文档产出与任务调度；上下文花在调度决策、跨 Story 记忆与用户交互上，不被单个 Story 的代码细节塞满。spec/impl 设计与 worktree/commit/归档等收尾工作由调度者亲自完成（需要全局视野与有状态上下文）。
2. **调研者/审查者/执行者平级，互不调用** — 三者均只与调度者通信，下级代理之间的任何流转（如执行者请审查者复核）都须经调度者派发。下级代理遇到阻塞（依赖缺失/设计冲突/权限不足）时，在结果报告中标注 BLOCKED + 原因反馈调度者，由调度者决策：拆任务 / 改方案 / 升级问用户。
3. **无状态交叉验证** — 每次调用都是全新上下文，看不到调度者的过程性思考，只看到显式传入的材料。这正是交叉验证的价值：审查者与执行者使用不同模型，避免单模型自审盲区。
4. **执行者可并行** — 调度者按 impl.md 预先切分**文件集不相交**的任务块后，可并行派发多个执行者；文件级冲突由调度者仲裁（预防优于仲裁：切分时保证不相交）。
5. **视觉解析者可选，取决于调度者模型的模态** — 工作流不限定模型选型：调度者的模型支持图片输入时，调度者直接读图自解，无需部署本角色；调度者为纯文本模型时，凡涉及图片一律派发视觉解析者（视觉模型），调度者只消费其结构化描述。图片以**文件绝对路径的文字形式**在任务书中传递。

## 任务书标准（调度者 → 下级代理的派发规范）

**impl.md 是唯一事实源**。任务书不内嵌实现细节，只包含四要素：

1. **执行范围**：impl.md 的哪些章节（如"决策 4/5 + 改动点 12-15"）
2. **工作目录**：worktree 绝对路径
3. **验证命令**：最小集（如编译指定模块 / 只跑新增测试类）——不跑全量测试/E2E/测试围栏（fence），那些由调度者在 Story 收尾统一执行
4. **交付物**：标准报告（改动清单 + 自验结果 + BLOCKED 项 + 发现项）

impl 写不清楚导致执行者做错，责任在调度者（文档质量）——这倒逼 impl 阶段做实，也是把「设计」与「执行」分离的价值边界。

## 调度协议

- **代理标识**：调度者经下级代理调用机制派发任务；代理标识 = 代理定义文件名，各角色的权限边界在代理定义中声明（定义归属实现仓）
- **材料显式传递**：需要下级代理读取的文件路径写进任务书，调度者在任务描述中说明各文件的角色
- **权限边界（三类原则）**：
  - 调研者：只读，严禁写任何文件
  - 审查者：只允许写 `{wf}/reviews/` 下的审查报告和 `{wf}/plans/{topic}/.stage`，严禁修改任何业务代码文件；可执行只读 git 命令与编译/测试命令（质量门全面审查用），严禁其他命令
  - 执行者：在 worktree 范围内编码，按任务书的最小验证命令自验，严禁扩大验证范围（全量测试/E2E 由调度者收尾统一执行）
- **产出落盘**：审查者直接写报告文件 + 更新 `.stage`；执行者的产出是代码变更 + 标准报告（不写文件）；stdout 回传结论摘要供调度者决策
- **无状态含义**：每次调用都是全新上下文，调度者需把执行基准（impl.md 章节 / 代码 diff / spec）显式传入任务书
- **并行规则**：调度者一次可发起多个调研/编码任务（编码需文件集不相交，作为并行派发前提，避免多执行者互踩同一文件）；审查必须在编码/文档产出完成后调用

## 检索工具职责（调研者专用）

外部调研使用三个只读检索通道，职责分工：

| 职责位 | 用途 | 说明 |
|---|---|---|
| **主搜索** | 网络搜索 | 外部技术方案检索的主通道 |
| **备选搜索** | 备选搜索 + 页面正文抽取 | 主搜索额度超额时切换使用 |
| **文档查询** | 库/框架文档查询 | 涉及库/框架用法时优先查证最新文档，即使自认熟悉——训练数据可能过时 |

> 具体命令、环境依赖与白名单配置归实现仓；本卷只约束职责分工与使用纪律（三个通道均为只读查询）。
