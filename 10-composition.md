# 10-composition —— 编码环境与规则组合

> 本卷职责：编码环境规范（前置条件 / worktree 纪律 / 依赖初始化 / 预热 / 清理）、Git 分支策略、测试围栏（fence）编排、规则组合覆盖。适用阶段：S-S9 / I-S6 编码前环境检查与质量门收尾编排。

## 编码环境

### 前置条件

- 读取 `.stage`，必须为 `IMPL_APPROVED`（Issue 为 `ISSUE_IMPL_APPROVED`）；不满足 → 拒绝执行："当前 topic 的 impl 审查尚未通过，请先完成 S-S8 / I-S5 审查"
- **同步主工作区到最新**：在项目 git 仓库根目录拉取主干分支最新，确保主工作区代码与远端一致——确保审查者后续读到的代码与 worktree 代码版本一致，避免因主工作区停留在旧分支导致审查者误判

### worktree 与新分支纪律

- 所有编码在独立 git worktree 中进行，不污染主工作区；worktree 需在项目 git 仓库根目录下创建
- 分支名使用 kebab-case，前缀按变更性质命名（`feat` / `fix` / `refactor` 等，见下文「Git 分支策略」）
- worktree 统一放在项目根之外的专用 worktree 目录下；因目录在项目根之外，**必须**预先将 worktree 目录加入执行者的路径写权限白名单（外部目录授权），否则执行者对 worktree 的读写会被拦截——未配置时先完成配置再进入编码

### 依赖初始化（best-effort）

- 进入 worktree 后按项目依赖清单自动检测并安装依赖，尽力而为
- 失败 → 提示用户手动安装后继续，不阻塞流程
- 具体包管理命令映射（依赖清单 → 初始化命令）归实现仓与用户配置，本卷不规定

### 审查期预热与放行解耦

- worktree 创建与依赖安装不依赖审查结论，可在调度者派发 Impl 审查（S-S8 / I-S5）的**同时**后台执行（可持久化后台会话：创建 worktree + 依赖安装 + 编译预热）
- 审查通过后执行者即刻开工，省去热身等待；审查不通过则废弃 worktree（移除 worktree + 删除分支，成本≈0）
- **边界**：`.stage` 仍须为 `IMPL_APPROVED` / `ISSUE_IMPL_APPROVED` 才能开始编码——预热只是环境准备，不是编码放行

### 同步与清理

- 在会话中以绝对路径操作 worktree 目录
- 同步回主仓库：本地 commit，每个 commit 能独立编译；推送远端须待质量门通过后由用户决定（见 09-check-split「质量门通过与失败处理」）
- 清理：移除 worktree + 删除分支

### 注意事项

- 每个 worktree 的依赖独立安装，不共享
- 同步项目规则（AGENTS.md）：拉取最新项目规则
- 多个 worktree 修改同一文件时 git merge 可能冲突，优先人工裁决

## Git 分支策略

### 分支命名（目标分支/性质/名称 三级格式）

| 目标 | 性质 | 分支前缀 | 合入目标 | 操作 |
|---|---|---|---|---|
| dev（开发） | feat/fix/chore | `feat/*`、`fix/*`、`chore/*` | 主干分支 | 从主干分支拉出，正常开发 |
| rel（测试发布） | feat/fix/chore | `rel/*` | 发布分支 | 从发布分支拉出，cherry-pick 干净 commit |
| ms（生产） | feat/fix/chore | `ms/*` | 主分支 | 从发布/主分支拉出，cherry-pick 干净 commit |

分支名与合入目标为可配置项（Git 三分支配置），默认即上表三级体系。

### 工作流（5 步）

1. **开发**：从主干分支拉出 `feat/xxx` 分支，在 worktree 中开发
2. **本地测试通过后**：整理 commit，确保每个 commit 能独立编译和通过测试
3. **推送 + MR**：推送分支到远端，创建合入主干分支的 MR
4. **发布测试环境**：从发布分支拉出 `rel/feat/xxx`，cherry-pick 干净 commit，创建合入发布分支的 MR
5. **发布生产**：从发布/主分支拉出 `ms/feat/xxx`，cherry-pick 干净 commit，创建合入主分支的 MR

### 注意事项

- CI 只关心 target 分支，不关心 source 分支
- 同一 feature 在 3 个环境各有独立分支（前缀区分），避免同名混淆
- 确保每个 commit 能独立编译和通过测试，避免协作冲突

## 测试围栏（fence）编排

- **冻结后即启动**：全部编码任务块完成后即代码事实冻结，调度者**立即**在可持久化后台会话启动 fence（全量单测 + E2E，按 Story 收尾一次的节奏，不在单个任务块中重复跑），**不阻塞等待**——同时继续文档归档与 commit 整理
- **与审查并行**：随即派发审查者做质量门全面审查（7 项清单见 09-check-split），与后台 fence 并行
- **汇合点**：审查者与 fence 均完成 → 双通过 → `.stage` → `DONE`；任一失败按下述规则处理后重新汇合。fence 结论以脚本生成的报告摘要为准（客观产物，审计链完整）
- **增量重跑三规则**：
  1. 审查者 REJECT + 修复仅涉测试代码/注释/文档 → fence 不重跑，审查者复审新 diff 即可
  2. 修复涉及生产代码 → fence 重跑（可持久化后台会话）+ 审查者复审，再次汇合
  3. fence 失败 → 修复后 fence 必重跑（串行模式下同样如此，并行未引入额外成本）

## 规则组合覆盖

- **加载顺序**：本仓规章（01~10 卷）→ 项目规则（项目根 AGENTS.md 等）→ 用户规则（user-rule 个人层）；后加载者覆盖先加载者的同名项
- **入仓筛选**："换人依然成立"测试——用户规则中的条目，仅在换一个人接手项目时依然成立，才可入仓（沉淀为项目规则或本仓规章）；否则属个人偏好，留在用户规则层，不入仓
- **模型选型归属**：各角色模型选择属用户规则个人层配置，规章正文不规定（角色定义见 08-roles）
