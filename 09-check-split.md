# 09-check-split —— 审查清单与检查分流

> 本卷职责：检查分流（机械项/语义项）、五类审查清单与深度分级、评分规则、规则注入机制、质量门结论处理。适用阶段：各审查环节（E-S5 / S-S5 / S-S8 / I-S5）与质量门（S-S10 / I-S7）。

## 检查分流

| 类别 | 检查项 | 执行者 |
|---|---|---|
| **机械项** | whitespace 检查、行数上限、commit 格式、状态转移表一致性（转移表定义见 07-state-machine）、编译、测试执行 | 工具（脚本/命令） |
| **语义项** | 范围对齐、术语一致性、隐藏依赖、风险兜底、回归面 | 审查者（模型判断） |

- 机械项以工具输出为结论依据，审查者引用工具结果，不重复人工比对
- 语义项无法被工具穷尽，必须由审查者结合基准材料逐项判断；工具不可替代

## 审查深度分级

| 审查类型 | 深度 | 要点 |
|---|---|---|
| Epic Spec 审查（E-S5） | 轻量 | 范围 + 致命遗漏 + Story 拆分合理性，报告 ≤40 行 |
| Spec 审查（S-S5） | 轻量 | 只做范围 + 致命遗漏 sanity check，报告 ≤30 行，快速放行 |
| Impl 审查（S-S8） | 完整 | 已有 spec + impl + 代码上下文，6 项清单深入核查，**把关重心在此** |
| Issue Impl 审查（I-S5） | 完整但精简 | 6 项清单，范围聚焦于 ≤3 文件，适配 Issue 规模 |
| 质量门（S-S10 / I-S7） | 全面 | 审查者一次性执行 7 项审查 |

## 五类审查清单

### Epic Spec 审查（3 项）

1. **范围边界** — 做什么、不做什么是否清晰？有无范围蔓延？
2. **致命遗漏** — 是否有明显未声明的核心依赖或架构级硬伤？
3. **Story 拆分合理性** — 路线图是否覆盖 Epic 全部范围？Story 粒度是否适中？依赖关系是否清晰？

### Spec 审查（2 项）

此阶段 impl 尚未产出、无实际代码可参照，仅做快速 sanity check，目标是挡住明显问题、快速放行进入 impl 阶段，把关重心放在 S-S8。

1. **范围边界** — 做什么、不做什么是否大致清晰？有无明显范围蔓延？
2. **致命遗漏** — 是否有明显未声明的核心依赖或架构级硬伤？

### Impl 审查（6 项）

1. **术语一致性** — impl 与 spec 术语是否对齐？有无歧义？
2. **范围对齐** — impl 是否完整覆盖 spec 的业务规则与验收标准？有无遗漏/越界？
3. **改动点核查** — 结合实际代码逐项核对 impl 中列出的改动点，确认路径、类名、方法签名是否真实存在且匹配；impl 中的行号标注为辅助定位，**不作为审查基准**（代码版本变动导致行号偏移，以方法名/注释/上下文描述定位为准）
4. **隐藏依赖** — 是否依赖未声明的外部系统/接口？是否调用了不存在的方法？
5. **风险与兜底** — 架构/性能/安全风险，异常分支、并发、事务、幂等等是否有兜底？
6. **回归面** — 改动是否波及既有功能？是否需要兼容处理？

### Issue Impl 审查（6 项，适配 Issue 规模）

审查方法与 Impl 审查相同（6 项清单），但范围更聚焦（改动文件 ≤3 个，审查深度适配 Issue 规模）。由于无独立 Spec 文档，"范围对齐"项以 S1 访谈确认的改动点为基准。

### 质量门全面审查（7 项）

1. **实现与 impl 一致性** — 实际改动是否与 impl 文档描述一致
2. **代码质量（OCR 流水线）** — 规则注入 + 逐文件审查 + 行级锚定 + 事实校验 + 精度优先：
   - 规则注入：按变更文件后缀加载对应语言规则（见「规则注入机制」）
   - 逐文件审查：对每个变更文件，结合 diff 与注入的语言规则逐项检查（死代码、逻辑错误、性能、线程安全等）
   - 行级锚定：每条问题定位到具体代码行（1~3 行上下文 + 起始行号）
   - 事实校验：完成后自检，diff 能直接证伪的评论剔除（只做证伪不做验证），降低误报
   - 精度优先：宁缺毋滥，只提确信是真缺陷的问题
3. **commit 信息** — 每个 commit message 简洁清晰、符合项目既有风格
4. **整体编译** — 优先引用并行执行的 fence 日志中的编译结果（fence 脚本含编译前置步骤）；fence 不可用或未启动时，才在 worktree 中自行执行编译
5. **受影响模块测试（fence）** — 测试围栏（fence）是质量门硬性卡点：必须执行项目的 fence 脚本，**不得以"无新增测试"为由跳过**（即使本次改动未新增测试，也需执行确认既有测试未被破坏）；若项目无 fence 脚本，则执行受影响模块的测试命令。fence 由调度者在代码冻结后于可持久化后台会话启动、与审查并行（编排规则见 10-composition）；审查者执行时 fence 可能尚未跑完，本项标注「fence 并行执行中，结论由调度者于汇合点核实 fence 报告摘要」
6. **测试覆盖与回归** — 新增测试已实现（impl 声明了测试计划时）；既有测试未破坏（以 fence 结果为依据）；fence 失败用例逐一分析是回归还是预期行为变更，不能笼统跳过；预期内行为变更提示执行者更新测试适配，意外回归提示执行者修复代码
7. **文档归档**（Story/Epic 适用，Issue 跳过）— spec/impl 已归档在 `{wf}/plans/{topic}/`；关键 feature 已记录到项目根 AGENTS.md

fence 结果摘要（总用例数/通过数/失败数/跳过数/耗时，分单元测试与 E2E 测试两行 + 合计）附入审查报告。

## 评分规则

- 扣分档位：critical -25 / high -12 / medium -5 / low -2
- 质量评分 = max(0, 100 - 总扣分)，最低 0 分

## 规则注入机制

- **机制**：按变更文件后缀映射加载对应语言规则文档，注入审查者的逐文件审查上下文（OCR 流水线第 1 步）
- **兜底边界**：无匹配规则的后缀使用默认规则文档兜底；语言规则库本体（各语言规则文档与后缀映射表）由实现仓拥有维护，本卷不展开内容

## 质量门通过与失败处理

- **通过**（7 项全部通过）：审查者写报告 + 更新 `.stage` → `DONE`；调度者提示用户「质量门通过，可以推送分支并创建 MR」，**不自动推送**（推送与 MR 由用户决定或用户明确要求时执行）
- **失败**（任一项不通过）：审查者写报告（列出所有问题）+ 更新 `.stage` → `WORKING`；调度者对报告问题做最终判定——确信是真缺陷的派发执行者修复；确信是误判的可跳过，跳过原因记录于项目规则（AGENTS.md）；存疑的默认修复。修复后重新递交，**全量重审**（不增量，确保收敛）；连续 3 轮质量门失败 → 提示用户人工介入

## 报告模板

五类审查报告格式见 `templates/`：`review-epic-spec-template.md`、`review-spec-template.md`、`review-impl-template.md`、`review-issue-impl-template.md`、`review-quality-gate-template.md`；审查轮次 N 计数规则见 06-artifacts。
