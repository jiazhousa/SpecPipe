# 审查报告: {topic} (Revision {N})

<!--
  模板规约：Impl 完整审查报告（S-S8，把关重心）。审查者直接写至 {wf}/reviews/{topic}-impl-revision-{N}.md；行数不设硬上限，问题完备优先。
  已有 spec 终稿 + impl 文档 + 调研产出的代码上下文，须结合实际代码深入核查。审查清单 6 项：
  ①术语一致性——impl 与 spec 术语对齐、无歧义；②范围对齐——完整覆盖 spec 业务规则与验收标准，无遗漏/越界；
  ③改动点核查——逐项核对路径/类名/方法签名真实存在且匹配（行号仅辅助定位，不作审查基准）；
  ④隐藏依赖——无未声明的外部系统/接口，未调用不存在的方法；⑤风险与兜底——架构/性能/安全风险，异常分支/并发/事务/幂等兜底；
  ⑥回归面——是否波及既有功能，是否需兼容处理。
-->

## 总体评价

<!-- 必填，1 行：通过 / 不通过 -->

## 发现的问题

<!-- 按 6 项清单逐项核对；每问题一条（标注所属清单项），无问题写「无」 -->
1. [问题] — 严重程度：critical/high/medium/low
   - 影响：[描述]
   - 建议：[修复建议]

## 结论

<!-- 必填，三选一 -->
# PASS / # REJECT / # REJECT: SPEC_OVERTURN

状态：{审查前状态} → {审查后状态}

<!--
  按转移表落定（落定前先校验 .stage 为 IMPL_REVIEWING，不一致则中止并提示调度者）：
  PASS → IMPL_APPROVED（不阻塞，直接进入编码）；REJECT → IMPL_DRAFT；SPEC_OVERTURN 经用户确认后回 SPEC_DRAFT，Epic 下 Story 需评估连锁回退。
-->
