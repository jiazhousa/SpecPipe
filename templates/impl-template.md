# Impl: [需求标题]

<!--
  模板规约：Story 路径实现方案文档，是编码任务书的唯一事实源。产出者：调度者；S-S6 产出（spec 放行后）。
  行数不设硬上限，以改动点完备为准；改动点须可切分为文件集不相交的任务块（并行派发的前提）。
-->

## 技术方案

<!-- 必填。总体策略与分块/分阶段安排；多方案对比与选型理由；关键时序（Git 操作、基线同步等） -->

## 改动点

<!-- 必填。逐文件列出改动说明（文件: 改动内容）。
     定位代码位置优先用方法名/类名/注释等语义描述；附行号须标「约」字且不作审查基准（规范见 06-artifacts.md）。
     涉及编码的任务块注明最小验证命令（编译指定模块 / 运行指定测试类等最小集，不跑全量） -->

## 依赖

<!-- 必填（无则写「无」）。外部依赖、前置条件、基线来源等 -->

## 风险

<!-- 必填（无则写「无」）。风险与缓解措施 -->
