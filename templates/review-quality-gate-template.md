# 质量门审查报告: {topic} (Revision {N})

<!--
  模板规约：质量门全面审查报告（S-S10 / I-S7 终检）。审查者直接写至 {wf}/reviews/{topic}-quality-gate-revision-{N}.md；
  在普通审查格式上增加质量评分与 fence 结果两节。审查清单 7 项：
  ①实现与 impl 一致性；②代码质量（OCR 流水线：规则注入 + 逐文件审查 + 行级锚定 + 事实校验 + 精度优先）；
  ③commit 信息（简洁清晰、符合项目既有风格）；④整体编译；⑤受影响模块测试（fence）；⑥测试覆盖与回归；⑦文档归档（Story/Epic 适用，Issue 跳过）。
  清单展开与检查分流见 09-check-split.md。
-->

## 总体评价

<!-- 必填，1 行：通过 / 不通过 -->

## 质量评分

<!-- 必填，1 行：{评分} / 100。扣分：critical -25 / high -12 / medium -5 / low -2；评分 = max(0, 100 - 总扣分) -->

## fence 结果

<!--
  必填。测试围栏（fence）结果摘要。fence 由调度者在代码冻结后后台并行启动——执行本审查时 fence 可能尚未跑完，
  此时标注「fence 并行执行中，结论由调度者于汇合点核实」并引用摘要文件路径（如 test-fence-reports/summary.txt）。
-->
- 单元测试：{用例数} 用例，{通过数} 通过，{失败数} 失败，{跳过数} 跳过，{耗时}
- E2E 测试：{用例数} 用例，{通过数} 通过，{失败数} 失败，{跳过数} 跳过，{耗时}
- 合计：{总用例数} 用例，{总通过数} 通过，{总失败数} 失败

## 发现的问题

<!-- 按 7 项清单逐项核对；每问题一条（标注所属清单项），无问题写「无」 -->
1. [问题] — 严重程度：critical/high/medium/low
   - 影响：[描述]
   - 建议：[修复建议]

## 结论

<!-- 必填，二选一（质量门无 SPEC_OVERTURN） -->
# PASS / # REJECT

状态：{审查前状态} → {审查后状态}

<!--
  按转移表落定（落定前先校验 .stage 为 QUALITY_GATE，不一致则中止并提示调度者）：
  PASS → DONE（前置条件 = 审查者 PASS 且调度者汇合核实 fence PASS）；REJECT → WORKING（执行者修复后重新递交，全量重审）。
-->
